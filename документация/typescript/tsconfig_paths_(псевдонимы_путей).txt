{
  "compilerOptions": {
    ...
    "baseUrl": ".", // влияет на то откуда будут разрешаться пути
    "paths": {

      "test-utils": ["/tests/react_testing_library/test-utils"], // псевдонимы путей и их расположение
      "myDir/*": ["./modules/*"] //  /* - помогает обращаться к папкам внутри модуля (на который ссылается псевдоним)

    }
  },
  "include": [
    "src",
    "tests"
  ], // псевдонимы будут работать только в модулях указанных здесь
}
K
также чтобы это все заработало нужно будет установить библиотеку tsconfig-paths
и при запуске файла (через ts-node) выполнять с командой -r tsconfig-paths/register
это маппит псевдонимы из tsconfig.ts с реальными путями